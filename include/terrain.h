#pragma once
#include "render.h"
#include "global.h"

// extern unsigned char terrain[(GAME_WIDTH * GAME_HEIGHT) / (TILE_SIZE * TILE_SIZE)];
extern unsigned char terrain[(GAME_WIDTH * GAME_HEIGHT) / (16 * 16)];
